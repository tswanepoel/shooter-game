{
  "name": "client",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "node scripts/dev.mjs",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "analyze:record": "node scripts/analyze-record.mjs",
    "analyze:jitter": "node scripts/compare-jitter.mjs",
    "analyze:ceiling": "node scripts/frame-ceiling.mjs",
    "analyze:mouse": "node scripts/mouse-rate.mjs",
    "analyze:tuning": "node scripts/tuning-analysis.mjs",
    "analyze:input": "node scripts/input-rates.mjs",
    "analyze:all": "npm run analyze:record && npm run analyze:jitter && npm run analyze:ceiling && npm run analyze:mouse && npm run analyze:tuning && npm run analyze:input"
  },
  "devDependencies": {
    "@types/three": "^0.185.0",
    "typescript": "~6.0.2",
    "vite": "^8.1.1"
  },
  "dependencies": {
    "three": "^0.185.1"
  }
}
